// Start of JS file
// Runs the main application while importing from user_input.js
// Would run the user input questions, and the general application.
const UI = require('./lib/user_input.js');

const prog = new UI();

prog.run();
// End of JS file