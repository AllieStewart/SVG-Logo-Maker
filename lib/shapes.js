// Start of JS file
// Would be the parent class to triangle.js, circle.js, square.js;
class Shapes {
    constructor(text, text_color, shape_color) {
        this.text = text;
        this.text_color = text_color;
        this.shape_color = shape_color;
      }
      // would need a function and return(s) possibly
      // for the other shapes?
}

module.exports = Shapes;
// End of JS file